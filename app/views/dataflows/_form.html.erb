<%= form_for(@dataflow) do |f| %>
  <% if @dataflow.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dataflow.errors.count, "error") %> prohibited this dataflow from being saved:</h2>

      <ul>
      <% @dataflow.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :dataset_id %><br />
    <%= f.number_field :dataset_id %>
  </div>
  <div class="field">
    <%= f.label :interface_id %><br />
    <%= f.number_field :interface_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
